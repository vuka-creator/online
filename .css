/* Reset */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}
body {
  font-family: 'Inter', sans-serif;
  line-height: 1.6;
  color: #333;
  background: #f9f9f9;
  scroll-behavior: smooth;
  transition: background 0.3s, color 0.3s;
}

/* Dark Mode */
body.dark {
  background: #121212;
  color: #eee;
}
body.dark .hero { background: linear-gradient(135deg, #1c1c3a, #0a0a2a); }
body.dark .skills li { background: #ff6b6b; }
body.dark .project-card { background: #1e1e1e; color: #eee; box-shadow: 0 8px 20px rgba(0,0,0,0.5);}
body.dark #ratings .rating-card { background: #ff6b6b; color: #fff; }

/* Dark Mode Toggle Button */
#darkModeToggle {
  position: fixed;
  top: 20px;
  right: 20px;
  background: #ff6b6b;
  border: none;
  color: #fff;
  padding: 10px 15px;
  border-radius: 50%;
  cursor: pointer;
  z-index: 100;
  font-size: 18px;
}

/* Buttons */
.btn {
  display: inline-block;
  padding: 12px 25px;
  margin-top: 15px;
  background: #ff6b6b;
  color: #fff;
  text-decoration: none;
  border-radius: 25px;
  transition: 0.3s;
  position: relative;
  overflow: hidden;
}
.btn:hover {
  background: #ff4757;
}
.btn:active { transform: scale(0.98); }

/* Hero */
.hero {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100vh;
  background: linear-gradient(135deg, #6b73ff, #000dff);
  color: #fff;
  text-align: center;
}
.hero h1 span { color: #ffd93d; }
.hero-content { opacity: 0; transform: translateY(20px); animation: fadeUp 1s forwards; animation-delay: 0.2s; }
.hero p { font-size: 1.2rem; margin-top: 10px; opacity: 0; transform: translateY(20px); animation: fadeUp 1s forwards; animation-delay: 0.4s; }

/* Sections */
section { padding: 80px 20px; }
section h2 { text-align: center; font-size: 2rem; margin-bottom: 40px; opacity: 0; transform: translateY(20px); animation: fadeUp 1s forwards; }

/* About Skills */
.skills { list-style: none; display: flex; justify-content: center; flex-wrap: wrap; }
.skills li { background: #ff6b6b; color: #fff; padding: 10px 20px; margin: 10px; border-radius: 20px; opacity: 0; transform: translateY(20px); animation: fadeUp 0.8s forwards; }
.skills li:nth-child(1){ animation-delay: 0.2s; }
.skills li:nth-child(2){ animation-delay: 0.4s; }
.skills li:nth-child(3){ animation-delay: 0.6s; }
.skills li:nth-child(4){ animation-delay: 0.8s; }
.skills li:nth-child(5){ animation-delay: 1s; }

/* Projects */
.projects-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: 30px; }
.project-card {
  background: #fff;
  border-radius: 12px;
  overflow: hidden;
  box-shadow: 0 8px 20px rgba(0,0,0,0.1);
  transition: transform 0.5s, box-shadow 0.5s;
  opacity: 0;
  transform: translateY(20px);
}
.project-card:hover { transform: translateY(-10px) scale(1.02); box-shadow: 0 12px 25px rgba(0,0,0,0.2);}
.project-card img { width: 100%; display: block; }
.project-card h3 { padding: 15px; color: #333; }
.project-card p { padding: 0 15px 15px; color: #666; }

/* Ratings Section */
#ratings { background: #fff7f7; text-align: center; }
.rating-card {
  max-width: 500px;
  margin: 0 auto;
  background: #ff6b6b;
  color: #fff;
  border-radius: 20px;
  padding: 40px 20px;
  box-shadow: 0 8px 20px rgba(0,0,0,0.1);
  display: flex;
  flex-direction: column;
  align-items: center;
  transition: transform 0.3s, box-shadow 0.3s;
}
.rating-card:hover { transform: translateY(-10px); box-shadow: 0 12px 25px rgba(0,0,0,0.2);}
.rating-score h3 { font-size: 4rem; margin-bottom: 10px; color: #ffd93d; }
.rating-score p { font-size: 1.2rem; }
.rating-description p { margin-top: 20px; color: #fff; font-size: 1rem; }

/* Testimonials */
#testimonials { background: #f9f9f9; }
.testimonial-card {
  max-width: 600px;
  margin: 20px auto;
  padding: 25px;
  background: #fff;
  border-radius: 12px;
  box-shadow: 0 8px 20px rgba(0,0,0,0.1);
  opacity: 0;
  transform: translateY(20px);
  transition: transform 0.3s, box-shadow 0.3s;
}
.testimonial-card p { font-style: italic; margin-bottom: 10px; }
.testimonial-card h4 { text-align: right; font-weight: 600; }

/* Skills Progress */
#skills-progress .skill { margin-bottom: 20px; max-width: 500px; margin-left: auto; margin-right: auto;}
#skills-progress .skill p { font-weight: 600; margin-bottom: 5px; }
.progress { background: #eee; border-radius: 20px; overflow: hidden; height: 20px; }
.progress div { background: #ff6b6b; height: 100%; width: 0; }

/* Contact */
#contact .socials { display: flex; justify-content: center; gap: 20px; margin-top: 20px; }
#contact .socials a { color: #333; text-decoration: none; font-weight: 600; transition: 0.3s; }
#contact .socials a:hover { color: #ff6b6b; }

/* Footer */
footer { text-align: center; padding: 20px; background: #333; color: #fff; }

/* Animations */
@keyframes fadeUp { to { opacity: 1; transform: translateY(0); } }